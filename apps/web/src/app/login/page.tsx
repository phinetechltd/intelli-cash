import React from "react";
import { LoginExperience } from "../../components/login-experience";

export default function LoginPage() {
  return (
    <LoginExperience
      copyText="Use the right access page for your role. Admin and partner workspaces now have separate sign-in doors while the public site stays focused on green enterprise services."
      copyTitle="Choose the right Intelli-Cash workspace"
    />
  );
}
