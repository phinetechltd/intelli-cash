{
  "name": "intellicash-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Intellicash web/API foundation for VSLA digitisation.",
  "engines": {
    "node": ">=22.12.0 <26.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w @intellicash/api\" \"npm run dev -w @intellicash/web\"",
    "start": "npm run start:render",
    "start:render": "npm run db:push:env -w @intellicash/api && npm run db:seed:if-empty -w @intellicash/api && tsx scripts/render-server.ts",
    "start:web": "npm run start -w @intellicash/web",
    "start:api": "npm run db:push:env -w @intellicash/api && npm run start -w @intellicash/api",
    "build": "npm run build -ws --if-present",
    "build:web": "npm run build -w @intellicash/web",
    "build:api": "npm run build -w @intellicash/api",
    "typecheck": "npm run typecheck -ws --if-present",
    "lint": "npm run lint -ws --if-present",
    "test": "npm run test -ws --if-present",
    "db:generate": "npm run db:generate -w @intellicash/api",
    "db:push": "npm run db:push -w @intellicash/api",
    "db:seed": "npm run db:seed -w @intellicash/api",
    "setup": "npm install && npm run db:push && npm run db:seed",
    "postinstall": "npm run db:generate -w @intellicash/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
